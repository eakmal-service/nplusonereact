#!/usr/bin/expect -f

set timeout 20
set host "72.61.229.171"
set user "root"
set password ")vbofQi/lHXSMqF?Nk8E"

spawn ssh -o StrictHostKeyChecking=no $user@$host

expect "password:"
send "$password\r"

expect "#"
send "ls -la /etc/nginx/sites-enabled/\r"

expect "#"
send "grep -r 'server_name' /etc/nginx/sites-enabled/\r"

expect "#"
send "grep -r 'listen' /etc/nginx/sites-enabled/\r"

expect "#"
send "exit\r"

expect eof
