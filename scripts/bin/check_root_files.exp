#!/usr/bin/expect -f

set timeout -1
set host "72.61.229.171"
set user "root"
set password ")vbofQi/lHXSMqF?Nk8E"

spawn ssh $user@$host
expect {
    "yes/no" { send "yes\r"; exp_continue }
    "password:" { send "$password\r" }
}

expect "#"
send "cd /var/www/nplusone\r"

# Check root files
expect "#"
send "ls -F\r"

# Check if public exists
expect "#"
send "ls -ld public\r"

# Check if .next/standalone exists
expect "#"
send "ls -ld .next/standalone\r"

expect "#"
send "exit\r"
expect eof
