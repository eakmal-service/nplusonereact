#!/usr/bin/expect -f
set timeout 60
set host "72.61.229.171"
set user "root"
set password ")vbofQi/lHXSMqF?Nk8E"

spawn ssh -o StrictHostKeyChecking=no $user@$host
expect "*?assword:*"
send "$password\r"
expect "*#*"

send "netstat -tulpn | grep -E ':80|:443'\r"
expect "*#*"

send "systemctl status apache2 --no-pager\r"
expect "*#*"

send "exit\r"
expect eof
