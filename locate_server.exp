#!/usr/bin/expect -f

set timeout 60

set vps_host "72.61.229.171"
set vps_user "root"
set vps_pass ")vbofQi/lHXSMqF?Nk8E"

spawn ssh $vps_user@$vps_host

expect {
    "yes/no" {
        send "yes\r"
        exp_continue
    }
    "password:" {
        send "$vps_pass\r"
    }
}

expect "#"
send "find /root/nplusone -name server.js\r"

expect "#"
send "ls -la /root/nplusone/nplus/.next\r"

expect "#"
send "exit\r"

expect eof
